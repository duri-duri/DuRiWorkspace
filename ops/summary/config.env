# === paths ===
REPO_ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
DESK_ROOT="/mnt/c/Users/admin/Desktop/두리백업"
USB_ROOT="/mnt/usb/두리백업"
BACKUP_SEARCH_DIRS="$DESK_ROOT:$USB_ROOT"

# === logs/output ===
SUMMARY_OUT="$REPO_ROOT/var/logs/system/summary/summary_$(date +%F).md"
LOG_ROOT="$REPO_ROOT/var/logs"
TZ="Asia/Seoul"

# === behavior ===
DRY_RUN=1                  # summary_report.sh 기본 DRY-RUN
FAIL_FAST=1
ALLOW_EXTENDED=1           # FULL 없으면 EXTENDED 허용
