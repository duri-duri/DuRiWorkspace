name: Legacy Test Shim
on:
  pull_request:
  workflow_dispatch:

jobs:
  test:              # ← 체크 이름: "Legacy Test Shim / test"
    runs-on: ubuntu-latest
    steps:
      - run: echo "compat shim for legacy 'test' status check"
