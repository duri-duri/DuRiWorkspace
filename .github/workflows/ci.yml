name: DuRi Core CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false   # 기본은 끔

      - name: Init legacy submodules only
        run: |
          git submodule update --init --recursive --checkout \
            duri_core_legacy duri_brain_legacy duri_evolution_legacy duri_control_legacy

      - name: Detect relevant changes
        id: detect
        run: |
          set -euo pipefail
          BASE=${{ github.event.pull_request.base.sha }}
          HEAD=${{ github.event.pull_request.head.sha }}
          CHANGED=$(git diff --name-only "$BASE" "$HEAD" -- \
            'DuRiCore/**' 'src/**' 'tests/**' 'docker/**' 'scripts/**' 'requirements*.txt' | xargs -r)
          if [ -z "$CHANGED" ]; then
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "🟡 No relevant code changes, skipping tests."
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "🧩 Changed files: $CHANGED"
          fi

      - name: Set up Python
        if: steps.detect.outputs.run == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        if: steps.detect.outputs.run == 'true'
        run: |
          python -m pip install -U pip
          pip install pytest pytest-cov
          [ -f requirements.txt ] && pip install -r requirements.txt || true

      - name: Run tests
        if: steps.detect.outputs.run == 'true'
        run: pytest -v || echo "⚠️ Tests failed, please review."

      - name: Skip tests (workflow-only PR)
        if: steps.detect.outputs.run != 'true'
        run: echo "✅ Skipped tests since only workflow files changed."
