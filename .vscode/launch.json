{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Terminal: Open in Current Directory",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/scripts/terminal_helper.js",
      "console": "integratedTerminal",
      "cwd": "${fileDirname}"
    }
  ]
}
